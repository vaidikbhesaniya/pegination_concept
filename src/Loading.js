import React from "react";

const Loading = () => {
  return (
    <div
      className="con"
      style={{
        background: "#202020 ",
        color: "white",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        width: "100vw",
        height: "100vh",
      }}
    >
      LOADING
    </div>
  );
};

export default Loading;
